<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.eightProject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.dicoding.eightProject.MyApplication"
        android:icon="@drawable/movie_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/movie_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TourismApp">

        <activity
            android:name="com.dicoding.eightProject.MainActivity"
            android:theme="@style/Theme.TourismApp.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.dicoding.eightProject.detail.DetailMovieActivity"
            android:label="@string/title_activity_detail_home"
            android:theme="@style/Theme.TourismApp.NoActionBar" />
    </application>

</manifest>